/** \brief obtinene dos enteres recibidos como parametros y los suma
 *
 * \param
 * \param rescibe operando X y Y int
 * \return res
 *
 */
int suma(int,int);

/** \brief obtinene dos enteres recibidos como parametros y los resta
 *
 * \param
 * \param rescibe operando X y Y int
 * \return res
 *
 */
int resta(int,int);

/** \brief obtinene dos enteres recibidos como parametros y los multiplica
 *
 * \param
 * \param rescibe operando X y Y int
 * \return Respuesta como res
 *
 */
int multiplica(int,int);

/** \brief obtinene dos enteres recibidos como parametros y los divide
 *
 * \param
 * \param rescibe operando X y Y int
 * \return Respuesta como res float
 *
 */
float division(int,int);

/** \brief obtinene un numero entero y calcula el factorial del mismo, como maximo 16
 *
 * \param
 * \param rescibe operando X
 * \return Respuesta como res int
 *
 */
int facto (int);
